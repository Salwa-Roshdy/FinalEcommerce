import { TermSplitPipe } from './term-split.pipe';

describe('TermSplitPipe', () => {
  it('create an instance', () => {
    const pipe = new TermSplitPipe();
    expect(pipe).toBeTruthy();
  });
});
